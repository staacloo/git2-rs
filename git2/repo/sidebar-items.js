initSidebarItems({"struct":[["Repository","An owned git repository, representing all state associated with the underlying filesystem.This structure corresponds to a `git_repository` in libgit2. Many other types in git2-rs are derivative from this structure and are attached to its lifetime.When a repository goes out of scope it is freed in memory but not deleted from the filesystem."],["RepositoryInitOptions","Options which can be used to configure how a repository is initialized"]]});